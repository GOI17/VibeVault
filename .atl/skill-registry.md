# Skill Registry

Generated by `sdd-init` for project **VibeVault**.

## Sources Scanned

- User skills: `~/.config/opencode/skills/` (also detected in `~/.claude/skills/`)
- Project skills: none found in `.claude/skills/`, `.gemini/skills/`, `.agent/skills/`, `skills/`
- Project convention docs: none found (`agents.md`, `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`, `copilot-instructions.md`)

## User Skills Trigger Table

| Skill | Trigger | Source |
|---|---|---|
| `branch-pr` | Creating/opening PRs, preparing changes for review | `~/.config/opencode/skills/branch-pr/SKILL.md` |
| `issue-creation` | Creating GitHub issues, bug reports, feature requests | `~/.config/opencode/skills/issue-creation/SKILL.md` |
| `judgment-day` | User asks for “judgment day”, “dual review”, “doble review”, “juzgar” | `~/.config/opencode/skills/judgment-day/SKILL.md` |
| `go-testing` | Writing Go tests, teatest/Bubbletea, test coverage in Go | `~/.config/opencode/skills/go-testing/SKILL.md` |
| `skill-creator` | User asks to create/edit AI skills | `~/.config/opencode/skills/skill-creator/SKILL.md` |

## Compact Rules

### rule: expo-react-native-typescript
- Stack is Expo + React Native + TypeScript (`strict: true`).
- Prefer `.ts/.tsx` and preserve TS path aliases (`@/*`, `@/domain/*`, `@/repositories/*`, `@/providers/*`).
- Keep navigation/layout wiring aligned with current React Navigation + stack setup in `app/_layout.tsx`.

### rule: repository-abstraction
- Respect domain repository interfaces under `src/domain/repositories/*`.
- Infrastructure adapters belong under `src/repositories/*` and implement domain interfaces.
- Dependency wiring is centralized in `src/providers/RepositoryProvider.tsx`.

### rule: data-validation-boundary
- Validate external/persisted data at boundaries (current pattern uses Zod in repositories/entities).
- Favor explicit parse/validation before mapping to domain entities.

### rule: quality-baseline
- Lint command available: `npm run lint` (Expo ESLint).
- Type-check command available: `npx tsc --noEmit`.
- No dedicated formatter command detected in `package.json`.

### rule: cmux-browser-evidence
- Use `cmux browser` for new visual/browser checks.
- Capture evidence as a small bundle: markdown summary, snapshot output, screenshot, and console log only when needed.
- Keep legacy `.playwright-mcp/` artifacts read-only.

### rule: no-playwright-for-this-change
- For the current change context, do not add new Playwright usage, scripts, or dependencies.
- If cmux is unavailable, mark the check as blocked and use the fallback described in `docs/browser-workflow.md`.


## Notes

- `sdd-*` skills were intentionally excluded from this registry index.
- No project-local convention docs were found, so compact rules are inferred from current code + tooling.
